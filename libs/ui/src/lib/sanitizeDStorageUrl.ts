

/**
 * Returns the IPFS link for a given hash.
 *
 * @param hash The IPFS hash.
 * @returns The IPFS link.
 */
const sanitizeDStorageUrl = (hash?: string): any=> {
  if (!hash) {
    return '';
  }

}


export default sanitizeDStorageUrl;